A series of AutoHotKey v2 scripts developed for personal use over the years. 

The most interesting is `CAPSLOCK Tap Hold.ahk` which causes the CAPSLOCK key to act as Esc if pressed on its own or as Ctrl if pressed as part of a chord. 
